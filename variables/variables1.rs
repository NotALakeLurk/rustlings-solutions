// variables1.rs
//
// Make me compile!
//
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a
// hint.

fn main() {
    // This one's obvious, x was never actually declared with let
    let x = 5;
    println!("x has the value {}", x);
}
